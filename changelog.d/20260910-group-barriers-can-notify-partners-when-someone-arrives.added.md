Group barriers now show live arrival progress on the wait overlay and a one-line banner on earlier pages when a partner is already waiting. Longer notice copy ellipsizes instead of wrapping over the prompt. Set ``notify_arrivals=False`` to keep the hold title only, or pass ``on_arrival_message`` to customize the copy.
