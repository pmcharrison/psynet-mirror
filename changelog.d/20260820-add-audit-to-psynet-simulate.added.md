Added ``psynet simulate --audit``, which zips ``data/simulated_data/`` to
``<audit>/artifacts/simulated_data.zip`` after the usual simulate export. Bare
``--audit`` auto-detects the packet; it does not mark the artifact present.
