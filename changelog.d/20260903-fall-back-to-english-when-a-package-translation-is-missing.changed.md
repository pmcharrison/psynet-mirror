Missing PsyNet catalog entries no longer abort feature-branch tests; the English source text is shown instead. Ordinary debug sessions still fail, so experiment authors notice missing translations. Catalogs are refreshed on the release branch. Tests that pin translated PsyNet framework copy run only on release branches.
