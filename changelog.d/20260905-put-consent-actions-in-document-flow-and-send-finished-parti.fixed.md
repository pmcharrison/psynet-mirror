Consent pages keep their agree/decline actions in the document flow instead of a fixed overlay that covered the last paragraphs and fought the themed footer. A finished participant who hits Back from the exit page is sent back to `/recruiter-exit` rather than a stale first timeline page: the exit navigation uses `location.replace`, `/timeline` responses are not stored in the back/forward cache, and the server redirects finished timeline visits.
