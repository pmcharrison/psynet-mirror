Hold-resume ``POST /response`` checks no longer wait out the timeline lock timeout when the last group member already holds that participant row, so the last arriver's first ``GET /timeline`` is not frozen on Starting experiment.... Ordinary Next submits still wait up to the lock timeout and retry one busy 503.
