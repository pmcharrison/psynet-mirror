Added a `psynet scripts` command group (`scaffold`, `update`, and `prune`) for managing experiment boilerplate, converted the `features/api` and `experiments/vocabulary_test` demos to essential-file layouts with round-trip CI coverage, and kept `psynet scaffold` / `psynet update-scripts` as compatibility aliases.
