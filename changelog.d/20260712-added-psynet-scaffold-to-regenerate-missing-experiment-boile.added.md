Added `psynet setup` and a `psynet scripts` command group (`scaffold`, `update`, and `prune`) for creating standalone experiment environments from minimal source demos, including active-version pinning, constrained dependency installation with `uv`, optional constraint skipping for monorepo CI, safe scaffold pruning, and a deprecated `psynet update-scripts` compatibility alias.
