Added `psynet setup` and a `psynet scripts` command group (`scaffold`, `update`, and `prune`) for creating standalone experiment environments from minimal source demos, including interactive editable-source handling, strict constrained dependency synchronization with `uv`, automatic bundled-demo preparation, optional constraint skipping for monorepo CI, safe scaffold pruning, and a deprecated `psynet update-scripts` compatibility alias.
