Added a `psynet experiment` command group (`scaffold`, `update`, and `prune`) for managing experiment boilerplate, including starter `experiment.py` / `requirements.txt` when missing, converted the `features/api` and `experiments/vocabulary_test` demos to essential-file layouts with round-trip CI coverage, and kept `psynet update-scripts` as a deprecated compatibility alias for `psynet experiment update`.
