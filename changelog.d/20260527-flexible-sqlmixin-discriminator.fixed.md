Fixed PsyNet custom SQL table polymorphic discriminator columns to use flexible string lengths, avoiding truncation errors for fully qualified polymorphic identities longer than 50 characters. Added regression coverage plus a two-participant Playwright test for the chatrooms demo that verifies live message relay and persisted chat history. (author: [Peter Harrison])
