Restyled the participant footer as page chrome rather than content. It now has its own faint tint (`--psynet-footer-bg`, defined for light and dark mode) instead of sharing the white content surface, and its contents line up with the page's content column rather than being spread across the whole window by Bootstrap's navbar spacing. The reward reads as a borderless figure with a small information glyph, matched in height to the buttons beside it, and `Comment` and `Exit` are outlined in the accent and danger colours so that their boundaries clear the 3:1 contrast that WCAG asks of a control's edge; a surface fill differed from the footer by only 1.07:1. Recolouring the footer means setting `--psynet-footer-bg`, and `--psynet-danger-soft` is available for quiet danger surfaces.
