Fixed the Prolific return-for-bonus path so failed participants whose
submission reaches ``AWAITING REVIEW`` (or is already ``APPROVED``) are
approved and marked terminal instead of leaving the study stuck.
