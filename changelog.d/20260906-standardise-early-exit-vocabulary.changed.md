Standardised early-leave vocabulary on ``early_exit`` across the API, config, DOM, routes, and database columns (``early_exited``), with participant-facing **Leave** labels unchanged. Deprecated ``show_abort_button`` / ``show_termination_button`` and related abort/terminate names in favour of ``show_early_exit_button``, ``min_reward_for_paid_early_exit``, ``#early-exit-button``, and ``/set_participant_as_early_exited/``. Error-page recovery uses ``allow_unpaid_early_exit_option=False`` so the unpaid below-threshold option is not offered. Shared the Leave confirmation modal markup between the timeline and error pages. Paid early exit eligibility is enforced server-side, with a one-shot error-page bypass.
