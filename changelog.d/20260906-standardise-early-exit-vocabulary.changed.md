Standardised early-leave vocabulary on ``early_exit`` across the API, config, DOM, routes, and database columns (``early_exited``), with participant-facing **Leave** labels unchanged. Deprecated ``show_abort_button`` / ``show_termination_button`` and related abort/terminate names in favour of ``show_early_exit_button``, ``min_reward_for_paid_early_exit``, ``#early-exit-button``, and ``/set_participant_as_early_exited/``. Early exits now use a server-owned ``EarlyExitPlan`` that records both the confirmation shown to the participant and the recruiter path executed after confirmation. Error recovery plans continue to offer the compensated recruiter path without repeating paid-exit eligibility checks. The timeline and error page share the same Leave confirmation modal.
