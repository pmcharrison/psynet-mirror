Standardised early-leave vocabulary on ``early_exit`` across the API, config, DOM, routes, and database columns (``early_exited``), with participant-facing **Leave** labels unchanged. Deprecated ``show_abort_button`` / ``show_termination_button`` and related abort/terminate names in favour of ``show_early_exit_button``, ``min_reward_for_paid_early_exit``, ``#early-exit-button``, and ``/set_participant_as_early_exited/``. Renamed ``force_compensated`` to ``skip_unpaid``.
