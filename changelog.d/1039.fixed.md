Sorted changelog fragments numerically by their integer ID prefix instead of lexicographically, so MR-numbered, synthetic migration (`9xxx`), and date-based (`YYYYMMDD`) fragments interleave predictably in `CHANGELOG.md` regardless of digit count (author: [Frank Höger])
