Footer Exit now opens an in-page recruiter-specific confirmation and fails participants who confirm, so Prolific uses the unsuccessful/partial-payment route while Lucid terminates the panel session. Experiments can customize the confirmation through ``Experiment.early_exit_confirmation``. The obsolete ad-page abort popup and route are removed; error pages provide the recovery path instead.
