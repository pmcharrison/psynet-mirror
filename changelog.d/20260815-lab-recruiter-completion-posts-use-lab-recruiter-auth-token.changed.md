Lab Recruiter completion and failure posts now send ``Authorization: Token <value>`` from the ``lab_recruiter_auth_token`` config key, falling back to the ``LAB_RECRUITER_AUTH_TOKEN`` environment variable. Deploying with the lab recruiter now requires one of these to be set. These posts now verify TLS certificates, time out after 30 seconds, and log failures without aborting participant submission.
