Added a tutorial on participant and trial failure, including that ``Participant.fail()`` can fail a completed participant, redirects still-working participants to the ``unsuccessful_end`` timeline branch, fails incomplete trials before fail routines run, and that Dallinger's post-submission data and attention checks are unused in PsyNet.
