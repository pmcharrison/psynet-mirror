Added a tutorial on participant and trial failure, including that ``Participant.fail()`` redirects the participant to the ``unsuccessful_end`` timeline branch, that incomplete trials are failed there before fail routines run, and that leaving the successful-end page without finishing still counts as an incomplete participant.
