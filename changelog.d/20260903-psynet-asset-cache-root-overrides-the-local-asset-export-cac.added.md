Added the ``PSYNET_ASSET_CACHE_ROOT`` environment variable to override the
default local asset export cache directory (``~/psynet-data/cache/assets``);
values are stripped, ``~`` is expanded, and relative paths are resolved before
use.
