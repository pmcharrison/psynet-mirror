Refactored experiment scaffolding and setup for clearer defaults and less duplication: ``config.txt`` is part of the default template set (create when missing, never overwrite), editable ``psynet setup`` reuses the shared scaffold helper, ``list_experiment_dirs`` shares ``_IN_REPO_EXPERIMENT_ROOTS``, setup/venv orchestration lives in ``psynet/experiment_setup.py``, PsyNet requirement parsing is shared between scaffold and CLI checks, the pytest experiment-directory fixture aligns with the runtime in-repo dependency gate, and thin wrappers such as ``is_bundled_demo`` / ``update_scripts_`` / duplicate pre-launch directory checks were removed.
