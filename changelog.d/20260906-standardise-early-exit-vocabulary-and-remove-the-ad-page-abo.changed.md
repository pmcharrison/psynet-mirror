Standardised early-leave vocabulary on ``early_exit`` across the API, config, DOM, routes, and database columns, so experiments now use ``show_early_exit_button``, ``min_reward_for_paid_early_exit``, ``#early-exit-button``, ``Participant.early_exited``, and ``/set_participant_as_early_exited/`` in place of the previous abort and terminate names. Participant-facing labels stay **Leave**. The ad-page abort popup and its route are removed: the timeline footer and error page now share one confirmation dialog.
