Release and deployment-test skills now cross-reference each other: RC validation requires a successful deployment test whose per-app analyses end with an explicit promotion verdict, release branches merged back into master must use a merge commit without deleting the source branch, and Slack release-summary guidance now asks agents to link concrete API/class names when rendered anchors exist.
