Documented two packaging constraints for adaptive experiments: keep item banks
outside Dallinger's excluded ``data/`` directory, and store model observations
on a dedicated table rather than extra columns on ``StaticTrial``.
