Reworked the deployment-test skill (renamed from `debug-deployment-test-experiments`) and coordinated it with the release skill: test deployments use a fresh branch per deployment named after the base tag (or `master-<short-hash>`), promoting an RC requires deployment tests with per-app promotion verdicts, release branches merge back into master via a merge commit without branch deletion, audit trails are archived in the private `computational-audition-lab/psynet-deployment-tests` repository, and each audit checks whether issues from the previous comparable deployment are resolved. `tests/deployment` is included in `list_experiment_dirs()` for release tooling but excluded from CI runs.
