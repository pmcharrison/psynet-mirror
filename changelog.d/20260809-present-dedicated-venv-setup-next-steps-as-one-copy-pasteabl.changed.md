Changed the ``psynet setup`` dedicated-``.venv`` prompt to print its remaining
steps as a single copy-pasteable shell block with explanatory comments,
including ``git init`` in its correct position when the directory is not yet a
Git repository.
