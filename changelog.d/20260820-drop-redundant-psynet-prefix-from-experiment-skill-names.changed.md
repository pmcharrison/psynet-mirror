Renamed experiment Agent Skills that used a redundant ``psynet-`` folder prefix
(for example ``psynet-experiment-implementation`` is now
``experiment-implementation``). The ``experiment/`` tree already namespaces them;
``explore-psynet-repository`` is unchanged because ``psynet`` is part of the
object, not a prefix.
