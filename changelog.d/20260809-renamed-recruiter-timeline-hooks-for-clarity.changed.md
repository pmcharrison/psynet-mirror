Renamed the recruiter timeline hooks `approve_assignment` to `submit_assignment` and (Prolific) `reject_assignment` to `request_return_for_bonus`; the old names were misleading because the methods submit the assignment (the completion code determines approval vs. screen-out payment) and request a return-for-bonus rather than rejecting anything. Custom recruiters overriding the old names must be updated.
