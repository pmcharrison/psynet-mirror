Graphics, vertical push-button lists, named colours, and footer clearance no longer break participant pages in the cases the default-theme review found. A `GraphicPrompt` keeps a minimum size on short viewports instead of collapsing to zero, vertical `PushButtonControl` lists stay in one scrolling column, `color="white"` remains CSS white so captions stay visible in dark mode, and body padding for the footer is applied only on pages that render one.
