Changed experiment setup UX: gate sync when the active environment is PsyNet's shared checkout ``.venv`` (with create-dedicated-``.venv``, prepare-files-only, and force-shared options), use numeric menus, add ``psynet setup --no-install`` and ``--docker``, move package upgrades to ``psynet installation update`` (``psynet update`` deprecated), and print clearer next-step / success messaging including soft Git guidance for standalone folders.
