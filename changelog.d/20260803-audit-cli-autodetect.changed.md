``psynet audit`` commands auto-detect the packet from the experiment directory
(``./audit.json`` or ``./audit/audit.json``), so ``validate .`` works from the
experiment root. Validate success copy now says the packet is coherent and
readiness may still be incomplete.
