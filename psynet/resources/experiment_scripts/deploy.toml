# Deployment selection is independent of Git.
version = 1

[exclude]
# Root-relative prefixes. data skips ./data, not static/data.
paths = [
    ".deploy",
    "data",
    "deploy",
    "deploy_logs",
    "develop",
    "local_only",
    "snapshots",
    "static/assets",
]
# Basenames skipped in every directory.
names = [
    ".env",
    ".pytest_cache",
    ".python-version",
    ".venv",
    "__pycache__",
    "logs.jsonl",
    "node_modules",
    "server.log",
    "source_code.zip",
]
# Literal endings such as .db, skipped in every directory.
suffixes = [
    ".db",
    ".dmg",
]
