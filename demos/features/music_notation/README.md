# README

`MusicNotationPrompt` renders staff notation from a compact string (here
`[E2B2]`). Reach for it whenever a page should show written music rather than
(or in addition to) audio.

## Usage

For instructions on how to run PsyNet experiments like this one, visit the
[PsyNet documentation](https://psynetdev.gitlab.io/PsyNet/).
