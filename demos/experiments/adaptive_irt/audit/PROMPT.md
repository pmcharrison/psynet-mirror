# Prompt

Dogfood the PsyNet experiment-implementation workflow by building a simple
adaptive IRT task. Follow `implement-experiment`, `make-experiment-adaptive`,
`power-analysis`, and `produce-experiment-audit`. Skip waiting for human plan
approval because this is infrastructure dogfooding, and record that choice in
the plan.
