These files are used in CI builds.

`dallinger-dev-requirements.txt` is a vendored snapshot of Dallinger's
`dev-requirements.txt` for the Dallinger version used by PsyNet's Docker
constraints build. When upgrading Dallinger in `pyproject.toml`, refresh this
file from the matching Dallinger release tag.
