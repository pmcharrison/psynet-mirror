{
    "fast.debug_launch.StaticFilesDebugLaunch.track_launch_time_s": {
        "code": "class StaticFilesDebugLaunch:\n    def track_launch_time_s(self, results, profile):\n        \"\"\"Return launch time for one static-file profile.\"\"\"\n        return results[profile]\n\n    def setup_cache(self):\n        \"\"\"Launch the static_big experiment once for each file profile.\"\"\"\n        from psynet.command_line import (\n            _start_local_server_and_wait_for_ready,\n            _stop_server,\n        )\n    \n        repo_root = Path(__file__).parents[2]\n        demo_dir = repo_root / \"tests/experiments/static_big\"\n        original_directory = Path.cwd()\n        results = {}\n    \n        try:\n            with _prepared_benchmark_experiment(demo_dir, repo_root):\n                os.chdir(demo_dir)\n                for profile, (count, file_size) in _STATIC_FILE_PROFILES.items():\n                    server_info = None\n                    with _temporary_static_payload(demo_dir, count, file_size):\n                        started_at = time.perf_counter()\n                        try:\n                            server_info = _start_local_server_and_wait_for_ready(\n                                [\"debug\", \"local\"]\n                            )\n                            results[profile] = time.perf_counter() - started_at\n                        finally:\n                            if server_info is not None:\n                                _stop_server(server_info)\n        finally:\n            os.chdir(original_directory)\n    \n        return results",
        "name": "fast.debug_launch.StaticFilesDebugLaunch.track_launch_time_s",
        "param_names": [
            "profile"
        ],
        "params": [
            [
                "'baseline'",
                "'many_small_files'",
                "'few_large_files'"
            ]
        ],
        "pretty_name": "Static files debug launch time",
        "setup_cache_key": "fast.debug_launch:148",
        "timeout": 180,
        "type": "track",
        "unit": "s",
        "version": "2"
    },
    "fast.export_benchmarks.IncrementalAssetTransfer.track_cold_transfer_time_s": {
        "code": "class IncrementalAssetTransfer:\n    def track_cold_transfer_time_s(self, results, profile):\n        \"\"\"Return wall time to hydrate an export with an empty asset cache.\"\"\"\n    \n        return results[profile][\"cold_transfer_time_s\"]\n\n    def setup(self, results, profile=None):\n        \"\"\"Prepare one parameterized run.\n    \n        ``profile`` is optional because ASV omits it from the call when\n        ``setup_cache`` returned ``None``; see\n        ``_skip_unless_canonical_export_supported``.\n        \"\"\"\n        _skip_unless_canonical_export_supported()\n\n    def setup_cache(self):\n        \"\"\"Run each transfer profile once and cache scalar metrics.\"\"\"\n        return _export_benchmark_cache(\n            lambda: {\n                name: _run_incremental_transfer_benchmark(profile)\n                for name, profile in _ASSET_EXPORT_PROFILES.items()\n            }\n        )",
        "name": "fast.export_benchmarks.IncrementalAssetTransfer.track_cold_transfer_time_s",
        "param_names": [
            "profile"
        ],
        "params": [
            [
                "'many_small_files'",
                "'few_large_files'"
            ]
        ],
        "pretty_name": "Incremental transfer time (cold cache)",
        "setup_cache_key": "fast.export_benchmarks:512",
        "timeout": 300,
        "type": "track",
        "unit": "s",
        "version": "1"
    },
    "fast.export_benchmarks.IncrementalAssetTransfer.track_warm_transfer_time_s": {
        "code": "class IncrementalAssetTransfer:\n    def track_warm_transfer_time_s(self, results, profile):\n        \"\"\"Return wall time to hydrate an export whose objects are already cached.\"\"\"\n    \n        return results[profile][\"warm_transfer_time_s\"]\n\n    def setup(self, results, profile=None):\n        \"\"\"Prepare one parameterized run.\n    \n        ``profile`` is optional because ASV omits it from the call when\n        ``setup_cache`` returned ``None``; see\n        ``_skip_unless_canonical_export_supported``.\n        \"\"\"\n        _skip_unless_canonical_export_supported()\n\n    def setup_cache(self):\n        \"\"\"Run each transfer profile once and cache scalar metrics.\"\"\"\n        return _export_benchmark_cache(\n            lambda: {\n                name: _run_incremental_transfer_benchmark(profile)\n                for name, profile in _ASSET_EXPORT_PROFILES.items()\n            }\n        )",
        "name": "fast.export_benchmarks.IncrementalAssetTransfer.track_warm_transfer_time_s",
        "param_names": [
            "profile"
        ],
        "params": [
            [
                "'many_small_files'",
                "'few_large_files'"
            ]
        ],
        "pretty_name": "Incremental transfer time (warm cache)",
        "setup_cache_key": "fast.export_benchmarks:512",
        "timeout": 300,
        "type": "track",
        "unit": "s",
        "version": "1"
    },
    "fast.export_benchmarks.LocalAssetExport.track_asset_export_time_s": {
        "code": "class LocalAssetExport:\n    def track_asset_export_time_s(self, results, profile):\n        \"\"\"Return wall time for the local asset export phase.\"\"\"\n    \n        return results[profile][\"asset_export_time_s\"]\n\n    def setup(self, results, profile=None):\n        \"\"\"Prepare one parameterized run.\n    \n        ``profile`` is optional because ASV omits it from the call when\n        ``setup_cache`` returned ``None``; see\n        ``_skip_unless_canonical_export_supported``.\n        \"\"\"\n        _skip_unless_canonical_export_supported()\n\n    def setup_cache(self):\n        \"\"\"Run each asset-export profile once and cache scalar metrics.\"\"\"\n        return _export_benchmark_cache(\n            lambda: {\n                name: _run_asset_export_benchmark(profile)\n                for name, profile in _ASSET_EXPORT_PROFILES.items()\n            }\n        )",
        "name": "fast.export_benchmarks.LocalAssetExport.track_asset_export_time_s",
        "param_names": [
            "profile"
        ],
        "params": [
            [
                "'many_small_files'",
                "'few_large_files'"
            ]
        ],
        "pretty_name": "Local asset export time",
        "setup_cache_key": "fast.export_benchmarks:555",
        "timeout": 300,
        "type": "track",
        "unit": "s",
        "version": "1"
    },
    "fast.export_benchmarks.LocalAssetExport.track_asset_file_count": {
        "code": "class LocalAssetExport:\n    def track_asset_file_count(self, results, profile):\n        \"\"\"Return the number of files exported by the asset benchmark.\"\"\"\n    \n        return results[profile][\"asset_file_count\"]\n\n    def setup(self, results, profile=None):\n        \"\"\"Prepare one parameterized run.\n    \n        ``profile`` is optional because ASV omits it from the call when\n        ``setup_cache`` returned ``None``; see\n        ``_skip_unless_canonical_export_supported``.\n        \"\"\"\n        _skip_unless_canonical_export_supported()\n\n    def setup_cache(self):\n        \"\"\"Run each asset-export profile once and cache scalar metrics.\"\"\"\n        return _export_benchmark_cache(\n            lambda: {\n                name: _run_asset_export_benchmark(profile)\n                for name, profile in _ASSET_EXPORT_PROFILES.items()\n            }\n        )",
        "name": "fast.export_benchmarks.LocalAssetExport.track_asset_file_count",
        "param_names": [
            "profile"
        ],
        "params": [
            [
                "'many_small_files'",
                "'few_large_files'"
            ]
        ],
        "pretty_name": "Local asset export files",
        "setup_cache_key": "fast.export_benchmarks:555",
        "timeout": 300,
        "type": "track",
        "unit": "files",
        "version": "1"
    },
    "fast.export_benchmarks.LocalAssetExport.track_asset_total_bytes": {
        "code": "class LocalAssetExport:\n    def track_asset_total_bytes(self, results, profile):\n        \"\"\"Return total bytes exported by the asset benchmark.\"\"\"\n    \n        return results[profile][\"asset_total_bytes\"]\n\n    def setup(self, results, profile=None):\n        \"\"\"Prepare one parameterized run.\n    \n        ``profile`` is optional because ASV omits it from the call when\n        ``setup_cache`` returned ``None``; see\n        ``_skip_unless_canonical_export_supported``.\n        \"\"\"\n        _skip_unless_canonical_export_supported()\n\n    def setup_cache(self):\n        \"\"\"Run each asset-export profile once and cache scalar metrics.\"\"\"\n        return _export_benchmark_cache(\n            lambda: {\n                name: _run_asset_export_benchmark(profile)\n                for name, profile in _ASSET_EXPORT_PROFILES.items()\n            }\n        )",
        "name": "fast.export_benchmarks.LocalAssetExport.track_asset_total_bytes",
        "param_names": [
            "profile"
        ],
        "params": [
            [
                "'many_small_files'",
                "'few_large_files'"
            ]
        ],
        "pretty_name": "Local asset export bytes",
        "setup_cache_key": "fast.export_benchmarks:555",
        "timeout": 300,
        "type": "track",
        "unit": "bytes",
        "version": "1"
    },
    "fast.export_benchmarks.LocalExport.track_data_row_count": {
        "code": "class LocalExport:\n    def track_data_row_count(self, results, profile):\n        \"\"\"Return the number of table rows written under ``database/``.\"\"\"\n    \n        return results[profile][\"data_row_count\"]\n\n    def setup(self, results, profile=None):\n        \"\"\"Prepare one parameterized run.\n    \n        ``profile`` is optional because ASV omits it from the call when\n        ``setup_cache`` returned ``None``; see\n        ``_skip_unless_canonical_export_supported``.\n        \"\"\"\n        _skip_unless_canonical_export_supported()\n\n    def setup_cache(self):\n        \"\"\"Run each export profile once and cache scalar metrics.\"\"\"\n        return _export_benchmark_cache(\n            lambda: {\n                name: _run_local_export_benchmark(profile)\n                for name, profile in _EXPORT_PROFILES.items()\n            }\n        )",
        "name": "fast.export_benchmarks.LocalExport.track_data_row_count",
        "param_names": [
            "profile"
        ],
        "params": [
            [
                "'static_big_single_bot'"
            ]
        ],
        "pretty_name": "Local export rows",
        "setup_cache_key": "fast.export_benchmarks:376",
        "timeout": 300,
        "type": "track",
        "unit": "rows",
        "version": "3"
    },
    "fast.export_benchmarks.LocalExport.track_database_size_bytes": {
        "code": "class LocalExport:\n    def track_database_size_bytes(self, results, profile):\n        \"\"\"Return the size of the exported table CSVs.\"\"\"\n    \n        return results[profile][\"database_size_bytes\"]\n\n    def setup(self, results, profile=None):\n        \"\"\"Prepare one parameterized run.\n    \n        ``profile`` is optional because ASV omits it from the call when\n        ``setup_cache`` returned ``None``; see\n        ``_skip_unless_canonical_export_supported``.\n        \"\"\"\n        _skip_unless_canonical_export_supported()\n\n    def setup_cache(self):\n        \"\"\"Run each export profile once and cache scalar metrics.\"\"\"\n        return _export_benchmark_cache(\n            lambda: {\n                name: _run_local_export_benchmark(profile)\n                for name, profile in _EXPORT_PROFILES.items()\n            }\n        )",
        "name": "fast.export_benchmarks.LocalExport.track_database_size_bytes",
        "param_names": [
            "profile"
        ],
        "params": [
            [
                "'static_big_single_bot'"
            ]
        ],
        "pretty_name": "Local export database size",
        "setup_cache_key": "fast.export_benchmarks:376",
        "timeout": 300,
        "type": "track",
        "unit": "bytes",
        "version": "3"
    },
    "fast.export_benchmarks.LocalExport.track_export_time_s": {
        "code": "class LocalExport:\n    def track_export_time_s(self, results, profile):\n        \"\"\"Return wall time for the local export command.\"\"\"\n    \n        return results[profile][\"export_time_s\"]\n\n    def setup(self, results, profile=None):\n        \"\"\"Prepare one parameterized run.\n    \n        ``profile`` is optional because ASV omits it from the call when\n        ``setup_cache`` returned ``None``; see\n        ``_skip_unless_canonical_export_supported``.\n        \"\"\"\n        _skip_unless_canonical_export_supported()\n\n    def setup_cache(self):\n        \"\"\"Run each export profile once and cache scalar metrics.\"\"\"\n        return _export_benchmark_cache(\n            lambda: {\n                name: _run_local_export_benchmark(profile)\n                for name, profile in _EXPORT_PROFILES.items()\n            }\n        )",
        "name": "fast.export_benchmarks.LocalExport.track_export_time_s",
        "param_names": [
            "profile"
        ],
        "params": [
            [
                "'static_big_single_bot'"
            ]
        ],
        "pretty_name": "Local export time",
        "setup_cache_key": "fast.export_benchmarks:376",
        "timeout": 300,
        "type": "track",
        "unit": "s",
        "version": "3"
    },
    "fast.function_benchmarks.SerializeRoundtrip.time_serialize": {
        "code": "class SerializeRoundtrip:\n    def time_serialize(self, cache):\n        serialize(cache[\"obj\"])\n\n    def setup_cache(self):\n        obj = _make_payload()\n        return {\"obj\": obj, \"encoded\": serialize(obj)}",
        "min_run_count": 2,
        "name": "fast.function_benchmarks.SerializeRoundtrip.time_serialize",
        "number": 0,
        "param_names": [],
        "params": [],
        "repeat": 0,
        "rounds": 2,
        "sample_time": 0.01,
        "setup_cache_key": "fast.function_benchmarks:32",
        "timeout": 120,
        "type": "time",
        "unit": "seconds",
        "version": "1",
        "warmup_time": -1
    },
    "fast.function_benchmarks.SerializeRoundtrip.time_unserialize": {
        "code": "class SerializeRoundtrip:\n    def time_unserialize(self, cache):\n        unserialize(cache[\"encoded\"])\n\n    def setup_cache(self):\n        obj = _make_payload()\n        return {\"obj\": obj, \"encoded\": serialize(obj)}",
        "min_run_count": 2,
        "name": "fast.function_benchmarks.SerializeRoundtrip.time_unserialize",
        "number": 0,
        "param_names": [],
        "params": [],
        "repeat": 0,
        "rounds": 2,
        "sample_time": 0.01,
        "setup_cache_key": "fast.function_benchmarks:32",
        "timeout": 120,
        "type": "time",
        "unit": "seconds",
        "version": "1",
        "warmup_time": -1
    },
    "fast.function_benchmarks.SerializeRoundtrip.track_encoded_size": {
        "code": "class SerializeRoundtrip:\n    def track_encoded_size(self, cache):\n        return len(cache[\"encoded\"])\n\n    def setup_cache(self):\n        obj = _make_payload()\n        return {\"obj\": obj, \"encoded\": serialize(obj)}",
        "name": "fast.function_benchmarks.SerializeRoundtrip.track_encoded_size",
        "param_names": [],
        "params": [],
        "setup_cache_key": "fast.function_benchmarks:32",
        "timeout": 120,
        "type": "track",
        "unit": "bytes",
        "version": "1"
    },
    "slow.experiment_performance.AsyncProcesses.track_median_queue_delay_ms": {
        "code": "class AsyncProcesses:\n    def track_median_queue_delay_ms(self, data, *param_values):\n        value = self._result_for(data, *param_values)[\"q_delay_median\"]\n        if value is None:\n            raise RuntimeError(\"performance test recorded no async process queue delay\")\n        return value * 1000.0\n\n    def setup_cache(self):\n        return super().setup_cache()",
        "name": "slow.experiment_performance.AsyncProcesses.track_median_queue_delay_ms",
        "param_names": [
            "n_bots",
            "duration_minutes"
        ],
        "params": [
            [
                "5"
            ],
            [
                "2.0"
            ]
        ],
        "pretty_name": "async_processes Median queue delay",
        "setup_cache_key": "slow.experiment_performance:209",
        "timeout": 1800,
        "type": "track",
        "unit": "ms",
        "version": "5"
    },
    "slow.experiment_performance.AsyncProcesses.track_median_response_time_ms": {
        "code": "class _BaseExperiment:\n    def track_median_response_time_ms(self, data, *param_values):\n        value = self._result_for(data, *param_values)[\"median_response_time\"]\n        if value is None:\n            raise RuntimeError(\"performance test recorded no median response time\")\n        return value * 1000.0\n\nclass AsyncProcesses:\n    def setup_cache(self):\n        return super().setup_cache()",
        "name": "slow.experiment_performance.AsyncProcesses.track_median_response_time_ms",
        "param_names": [
            "n_bots",
            "duration_minutes"
        ],
        "params": [
            [
                "5"
            ],
            [
                "2.0"
            ]
        ],
        "pretty_name": "async_processes Median request time",
        "setup_cache_key": "slow.experiment_performance:209",
        "timeout": 1800,
        "type": "track",
        "unit": "ms",
        "version": "5"
    },
    "slow.experiment_performance.Static.track_median_response_time_ms": {
        "code": "class _BaseExperiment:\n    def track_median_response_time_ms(self, data, *param_values):\n        value = self._result_for(data, *param_values)[\"median_response_time\"]\n        if value is None:\n            raise RuntimeError(\"performance test recorded no median response time\")\n        return value * 1000.0\n\nclass Static:\n    def setup_cache(self):\n        return super().setup_cache()",
        "name": "slow.experiment_performance.Static.track_median_response_time_ms",
        "param_names": [
            "n_bots",
            "duration_minutes"
        ],
        "params": [
            [
                "5"
            ],
            [
                "2.0"
            ]
        ],
        "pretty_name": "static Median request time",
        "setup_cache_key": "slow.experiment_performance:189",
        "timeout": 1800,
        "type": "track",
        "unit": "ms",
        "version": "5"
    },
    "slow.experiment_performance.StaticBig.track_median_response_time_ms": {
        "code": "class _BaseExperiment:\n    def track_median_response_time_ms(self, data, *param_values):\n        value = self._result_for(data, *param_values)[\"median_response_time\"]\n        if value is None:\n            raise RuntimeError(\"performance test recorded no median response time\")\n        return value * 1000.0\n\nclass StaticBig:\n    def setup_cache(self):\n        return super().setup_cache()",
        "name": "slow.experiment_performance.StaticBig.track_median_response_time_ms",
        "param_names": [
            "n_bots",
            "duration_minutes"
        ],
        "params": [
            [
                "5"
            ],
            [
                "2.0"
            ]
        ],
        "pretty_name": "static_big Median request time",
        "setup_cache_key": "slow.experiment_performance:200",
        "timeout": 1800,
        "type": "track",
        "unit": "ms",
        "version": "5"
    },
    "slow.experiment_performance.Timeline.track_median_response_time_ms": {
        "code": "class _BaseExperiment:\n    def track_median_response_time_ms(self, data, *param_values):\n        value = self._result_for(data, *param_values)[\"median_response_time\"]\n        if value is None:\n            raise RuntimeError(\"performance test recorded no median response time\")\n        return value * 1000.0\n\nclass Timeline:\n    def setup_cache(self):\n        return super().setup_cache()",
        "name": "slow.experiment_performance.Timeline.track_median_response_time_ms",
        "param_names": [
            "n_bots",
            "duration_minutes"
        ],
        "params": [
            [
                "12"
            ],
            [
                "8.0"
            ]
        ],
        "pretty_name": "timeline Median request time",
        "setup_cache_key": "slow.experiment_performance:179",
        "timeout": 1800,
        "type": "track",
        "unit": "ms",
        "version": "5"
    },
    "version": 2
}