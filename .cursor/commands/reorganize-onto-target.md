# Reorganize Onto Target

This command is `/reorganize-onto-master`. Follow
`.cursor/skills/reorganize-onto-master/SKILL.md`.

It rebuilds the branch on the open merge request's target, not always
`master`.
