---
name: update-onto-master
description: >-
  Alias for /update-onto-target: merge the open GitLab merge-request
  target (often master) into the feature branch. Use when the user
  runs /update-onto-master or asks to sync with master.
---

# Update Onto Master

Follow `.cursor/skills/update-onto-target/SKILL.md` in full.

The merge-request target is not always `master`. Do not merge
`origin/master` unless that is the MR target.
